{{<layout}}
{{$content}}
	<img src="{{cover}}">
	<div>{{title}}</div>
	<strong>No of copies: 
		<esi:include src="{{ stockApiUrl }}/stock/{{ isbn }}">
			<a href="{{ stockApiUrl }}/stock/{{ isbn }}" class="include">No of copies</a>
		</esi:include>
	</strong>
{{/content}}
{{/layout}}
